<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Custom signature permission for inter-app security -->
    <permission
        android:name="com.gg_tech_bharat.gremaider.permission.WRITE_REMINDERS"
        android:protectionLevel="signature" />
    <uses-permission android:name="com.gg_tech_bharat.gremaider.permission.WRITE_REMINDERS" />

    <!-- Permissions -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.DISABLE_KEYGUARD" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.READ_CALL_LOG" />
    <uses-permission android:name="android.permission.READ_SMS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/iconofgremainder"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.GRemaider">

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Full-Screen Reminder Alarm Activity -->
        <activity
            android:name=".ui.ReminderAlertActivity"
            android:excludeFromRecents="true"
            android:launchMode="singleInstance"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:theme="@style/Theme.GRemaider"
            android:exported="false" />

        <!-- Broadcast Receiver for Reminder Alarms -->
        <receiver
            android:name=".receiver.ReminderReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.gg_tech_bharat.gremaider.ACTION_ALARM_TRIGGER" />
                <action android:name="com.gg_tech_bharat.gremaider.ACTION_COMPLETE" />
                <action android:name="com.gg_tech_bharat.gremaider.ACTION_SNOOZE" />
            </intent-filter>
        </receiver>

        <!-- Broadcast Receiver for Boot Events -->
        <receiver
            android:name=".receiver.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Remote API Broadcast Receiver for Dialer Integration -->
        <receiver
            android:name=".receiver.RemoteApiReceiver"
            android:permission="com.gg_tech_bharat.gremaider.permission.WRITE_REMINDERS"
            android:exported="true">
            <intent-filter>
                <action android:name="com.gg_tech_bharat.gremaider.ACTION_ADD_REMINDER" />
            </intent-filter>
        </receiver>

        <!-- Phone Call & SMS Quick Response Receiver -->
        <receiver
            android:name=".receiver.PhoneCallReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.PHONE_STATE" />
                <action android:name="com.gg_tech_bharat.gremaider.ACTION_CHECK_MISSED_CALL" />
            </intent-filter>
        </receiver>

        <!-- App Widget Provider -->
        <receiver
            android:name=".widget.ReminderWidgetProvider"
            android:exported="true"
            android:label="GReminder Widget">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/reminder_widget_info" />
        </receiver>

    </application>

</manifest>